import { Kode_Mono, Inter, Sedan } from "next/font/google";

export const kode = Kode_Mono({ weight: "400", subsets: ["latin"] });
export const inter = Inter({ subsets: ["latin"] });
export const sedan = Sedan({ weight: "400", subsets: ["latin"] });